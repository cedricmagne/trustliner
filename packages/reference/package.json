{
  "name": "@trustline-onboarder/reference",
  "version": "0.0.0",
  "private": true,
  "description": "Reference implementation of trustline-free onboarding — issuer/sender and recipient flows.",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@stellar/stellar-sdk": "^13.0.0",
    "@trustline-onboarder/sdk": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
